# gerer les cas où il y a des dates, des facteurs
# gerer le drop de niveaux en cas de besoin


# replace_as_na <- function(eldata, na_pattern = c(" "), verbose=TRUE){
#
#   if (verbose) {}
#   invisible(eldata)
# }
#
# replace_as_na_vec <- funtion(vec, na_pattern = c(" "),  verbose = TRUE){
#   if(is.factor(vec)) {
#     vec %in% na_pattern <- NA
# }
